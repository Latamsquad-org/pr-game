# Landing sin “index.php” en la URL (Hostinger / Apache)
DirectoryIndex index.php

<IfModule mod_rewrite.c>
RewriteEngine On

# Si alguien entra a /index.php, redirigir a la raíz limpia
RewriteCond %{THE_REQUEST} \s/+([^?\s]*?)index\.php[?\s] [NC]
RewriteRule ^index\.php$ ./ [R=301,L]
</IfModule>
