# Review T4
Base: 661e8acaa0a4dfa7a1677f7b28bc94eba1c3dc1d
Head: c58ac12c3aeb2266ec483b16339b9211b6c476e1

## Commits
c58ac12 Activa el formulario de Trafico con CSRF y confirmacion.

## Stat
 docs/nginx-templates/admin/_bootstrap.php |  20 ++++++  docs/nginx-templates/admin/traffic.php    | 103 +++++++++++++++++++++++++++++-  docs/nginx-templates/assets/css/site.css  |  82 ++++++++++++++++++++++++  3 files changed, 204 insertions(+), 1 deletion(-)

## Diff
```diff
diff --git a/docs/nginx-templates/admin/_bootstrap.php b/docs/nginx-templates/admin/_bootstrap.php index f546459..9ae6a94 100644 --- a/docs/nginx-templates/admin/_bootstrap.php +++ b/docs/nginx-templates/admin/_bootstrap.php @@ -26,10 +26,30 @@ function admin_safe_return_path(?string $path): string          return '/admin/';      }      return $path;  }   +/** + * Session CSRF token for admin forms. + */ +function admin_csrf_token(): string +{ +    if (empty($_SESSION['admin_csrf']) || !is_string($_SESSION['admin_csrf'])) { +        $_SESSION['admin_csrf'] = bin2hex(random_bytes(16)); +    } +    return $_SESSION['admin_csrf']; +} + +/** + * Validate posted CSRF token against session. + */ +function admin_csrf_validate(?string $token): bool +{ +    $sess = $_SESSION['admin_csrf'] ?? ''; +    return is_string($token) && is_string($sess) && $sess !== '' && hash_equals($sess, $token); +} +  auth_start_session();  auth_send_noindex();    $discordId = auth_current_discord_id();  if ($discordId === null) { diff --git a/docs/nginx-templates/admin/traffic.php b/docs/nginx-templates/admin/traffic.php index 339b1cf..32eede7 100644 --- a/docs/nginx-templates/admin/traffic.php +++ b/docs/nginx-templates/admin/traffic.php @@ -1,7 +1,108 @@  <?php  declare(strict_types=1);  require_once __DIR__ . '/_bootstrap.php';  require_once __DIR__ . '/_layout.php'; +require_once __DIR__ . '/lib/traffic_settings.php'; +require_once __DIR__ . '/lib/traffic_nginx.php'; + +$flash = ''; +$flashErr = false; +$settings = traffic_settings_load(); +$showConfirm = false; +$lastBackup = ''; + +if ($_SERVER['REQUEST_METHOD'] === 'GET') { +    $lastBackup = isset($_SESSION['traffic_last_backup']) && is_string($_SESSION['traffic_last_backup']) +        ? $_SESSION['traffic_last_backup'] +        : ''; +    unset($_SESSION['traffic_last_backup']); +} + +if ($_SERVER['REQUEST_METHOD'] === 'POST') { +    if (!admin_csrf_validate($_POST['csrf'] ?? null)) { +        http_response_code(403); +        $flash = 'CSRF invalido'; +        $flashErr = true; +    } else { +        $parsed = traffic_settings_validate($_POST); +        $settings = $parsed['settings']; +        if (!$parsed['ok']) { +            $flash = implode('; ', $parsed['errors']); +            $flashErr = true; +        } elseif (empty($_POST['confirm'])) { +            $showConfirm = true; +        } else { +            $result = traffic_nginx_apply($settings); +            $flash = $result['message']; +            $flashErr = !$result['ok']; +            if (!empty($result['nginx_log']) && $flashErr) { +                $flash .= ' | ' . $result['nginx_log']; +            } +            if ($result['ok']) { +                $settings = traffic_settings_load(); +                if (!empty($result['backup']) && is_string($result['backup'])) { +                    $_SESSION['traffic_last_backup'] = $result['backup']; +                } +            } +        } +    } +} +  admin_render_start('traffic', 'Trafico | Admin LATAMFILES', 'Trafico'); -echo '<p class="latam-admin__soon">Proximamente: limites de trafico y descargas en Nginx.</p>'; + +if ($flash !== '') { +    $flashClass = $flashErr +        ? 'latam-admin__flash latam-admin__flash--err' +        : 'latam-admin__flash latam-admin__flash--ok'; +    echo '<p class="' . admin_h($flashClass) . '">' . admin_h($flash) . '</p>'; +} + +if ($lastBackup !== '') { +    echo '<p class="latam-admin__meta">Ultimo backup: ' . admin_h($lastBackup) . '</p>'; +} + +$csrf = admin_csrf_token(); +?> +<form class="latam-admin-form" method="post" action="/admin/traffic.php"> +  <input type="hidden" name="csrf" value="<?= admin_h($csrf) ?>"> + +  <label class="latam-admin-form__row latam-admin-form__row--check"> +    <input type="checkbox" name="enabled" value="1"<?= $settings['enabled'] ? ' checked' : '' ?>> +    <span>Limites activos</span> +  </label> + +  <label class="latam-admin-form__row"> +    <span class="latam-admin-form__label">Conexiones demos por IP</span> +    <input class="latam-admin-form__input" type="number" name="demo_conn_per_ip" min="1" max="10" step="1" +      value="<?= admin_h((string) $settings['demo_conn_per_ip']) ?>" required> +  </label> + +  <label class="latam-admin-form__row"> +    <span class="latam-admin-form__label">Velocidad max MB/s</span> +    <input class="latam-admin-form__input" type="number" name="demo_rate_mbs" min="1" max="50" step="0.1" +      value="<?= admin_h((string) $settings['demo_rate_mbs']) ?>" required> +  </label> + +  <label class="latam-admin-form__row"> +    <span class="latam-admin-form__label">Peticiones listado por minuto</span> +    <input class="latam-admin-form__input" type="number" name="autoindex_req_per_min" min="10" max="300" step="1" +      value="<?= admin_h((string) $settings['autoindex_req_per_min']) ?>" required> +  </label> + +  <?php if ($showConfirm): ?> +  <section class="latam-admin-form__confirm" aria-label="Confirmar cambios"> +    <p class="latam-admin-form__confirm-text">Aplicar y recargar Nginx?</p> +    <input type="hidden" name="confirm" value="1"> +    <div class="latam-admin__actions"> +      <button type="submit" class="latam-admin__btn latam-admin__btn--primary">Si, aplicar y recargar</button> +      <a class="latam-admin__btn" href="/admin/traffic.php">Cancelar</a> +    </div> +  </section> +  <?php else: ?> +  <div class="latam-admin__actions"> +    <button type="submit" class="latam-admin__btn latam-admin__btn--primary">Guardar</button> +  </div> +  <?php endif; ?> +</form> +<?php  admin_render_end(); diff --git a/docs/nginx-templates/assets/css/site.css b/docs/nginx-templates/assets/css/site.css index a4f4490..1b3ca7b 100644 --- a/docs/nginx-templates/assets/css/site.css +++ b/docs/nginx-templates/assets/css/site.css @@ -507,10 +507,92 @@ a:focus-visible {    padding: 1rem 1.1rem;    border: 1px dashed var(--latam-border);    color: var(--latam-text-muted);  }   +.latam-admin__flash { +  margin: 0 0 1rem; +  padding: 0.75rem 1rem; +  border: 1px solid var(--latam-border); +} + +.latam-admin__flash--ok { +  border-color: var(--latam-accent); +  color: #fff; +} + +.latam-admin__flash--err { +  border-color: #8b3a3a; +  color: #ffb4b4; +} + +.latam-admin__meta { +  margin: 0 0 1rem; +  color: var(--latam-text-muted); +  font-size: 0.9rem; +} + +.latam-admin-form { +  max-width: 28rem; +  display: flex; +  flex-direction: column; +  gap: 1rem; +  margin-top: 1rem; +} + +.latam-admin-form__row { +  display: flex; +  flex-direction: column; +  gap: 0.35rem; +} + +.latam-admin-form__row--check { +  flex-direction: row; +  align-items: center; +  gap: 0.5rem; +  font-weight: 700; +} + +.latam-admin-form__label { +  font-size: 0.85rem; +  font-weight: 700; +  letter-spacing: 0.04em; +  color: var(--latam-text-muted); +} + +.latam-admin-form__input { +  padding: 0.5rem 0.65rem; +  border: 1px solid var(--latam-border); +  background: rgba(11, 15, 12, 0.85); +  color: var(--latam-text); +  font: inherit; +  max-width: 12rem; +} + +.latam-admin-form__input:focus-visible { +  outline: none; +  border-color: var(--latam-accent); +} + +.latam-admin-form__confirm { +  margin-top: 0.5rem; +  padding: 1rem 1.1rem; +  border: 1px solid var(--latam-accent); +  background: rgba(107, 143, 60, 0.12); +} + +.latam-admin-form__confirm-text { +  margin: 0 0 0.75rem; +  font-weight: 700; +} + +button.latam-admin__btn { +  cursor: pointer; +  font: inherit; +  background: transparent; +} +  @media (max-width: 720px) {    .latam-admin {      grid-template-columns: 1fr;    }  
```
